# Supabase backend — client (publishable) config.
# Copy to .env.local for local dev. Without these, the app runs fully
# local (localStorage) with no backend sync.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
